spring:
  profiles:
    active: test # 이 파일이 활성화되었음을 명시
  datasource:
    url: jdbc:h2:mem:erp_test_db;MODE=MySQL # 메모리 DB 사용 (독립적임)
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop # 테스트 시작 시 테이블 생성, 종료 시 삭제
    show-sql: true